# yaml-language-server: $schema=../../../../yuruna/test/schemas/sequence.schema.yml

# LICENSEURI https://yuruna.link/license
# Copyright (c) 2019-2026 by Alisson Sol et al.

sequenceGuid: 426ca572-7dda-4e43-be71-93462c7ef6e7
sequenceRevision: 2

description: "Ubuntu Server: log in, install Kubernetes, prepare for text-to-sql deployment."
keystrokeMechanism: gui
# Chain start.guest directly. The workload.guest chain
# additionally installs VS Code (ubuntu.server.24.code.sh), which text-to-sql
# does not use and only adds install time + failure surface to an unattended
# cycle. update.sh already leaves pwsh + the framework/project repos in place.
resource:
  ubuntu.server.24:
    - start.guest.ubuntu.server.24
variables:
  username: yt2sqluser
  hostname: yt2sqlhost

component:
  - action: retry
    maxAttempts: 2
    description: "Login + retry"
    steps:
      - snippet: firstLoginPrime

      - action: passwdPrompt
        pattern: "login:"
        timeoutSeconds: 600
        text: "${username}"
        description: "OCR: login:"

      - action: passwdPrompt
        pattern: "Password:"
        text: "${ext:authentication.GetPassword(${username})}"
        description: "OCR: Password:"

  - action: waitForText
    pattern: "${username}@${hostname}"
    timeoutSeconds: 90
    description: "OCR: ${username}@${hostname}"

workload:
  - action: fetchAndExecute
    text: "/usr/local/lib/yuruna/fetch-and-execute.sh guest/ubuntu.server.24/ubuntu.server.24.k8s.sh"
    description: "ubuntu.server.24.k8s.sh"
    waitPattern: "FETCHED AND EXECUTED:"
    timeoutSeconds: 1800

  # `&&`, not `;`: the node table must be reachable ONLY when the wait
  # actually succeeded, so that seeing the table is itself proof of readiness.
  - action: inputTextAndEnter
    text: "kubectl wait --for=condition=Ready nodes --all --timeout=300s && clear && kubectl get nodes"
    description: "Check node status"

  # Gate on "control-plane" -- it appears only in the ROLES column of the
  # cleared `kubectl get nodes` table above. Do NOT gate on "Ready":
  # Test-OCRMatch is fuzzy, so "Ready" matches the echoed
  # "--for=condition=Ready" of the command itself (passing before the wait has
  # even run, then letting later steps type into a still-blocked shell) and it
  # also matches inside the word "NotReady". timeoutSeconds must exceed the
  # 300s wait being watched, or a healthy but slow cluster times out here.
  - action: waitForText
    pattern: "control-plane"
    timeoutSeconds: 360
    description: "OCR: control-plane"

  - action: inputTextAndEnter
    text: "clear; newgrp docker"
    description: "Activate docker group"

  - action: saveDiskSnapshot
    id: "k8s.text-to-sql"
    description: "Snapshot before Postgres in text-to-sql workload"
